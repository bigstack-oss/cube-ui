/**
 * Brand palettes shipped as `themes/*.css`. Add a new entry here, a matching
 * `your-theme.css` file in `themes/`, and an `@import` for it in
 * `themes/index.css`, for each additional product palette.
 */
export type CubeThemeName = 'cubeCOS' | 'cubeEMP'
